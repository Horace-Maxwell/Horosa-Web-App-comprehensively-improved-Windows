// AI 分析 · 操作手册(帮助页内容组件,AI 分析页右上角「帮助」打开)。
// 把顶栏全部控件、右侧每个页签、挂载与上下文口径、接口/模型参数写清楚;中性表述,纯展示零后端。
// 共享样式见 helpDocStyle;kv 行本组件自渲染。绝不写章节号、来源书名/软件名、内部标识、后端地址。
import { Component } from 'react';
import { Tabs } from 'antd';

const { TabPane } = Tabs;
import { MUTED, h, p, ul, li, card, ct, body, title } from './helpDocStyle';

const kv = (k, v) => <div style={{ margin: '1px 0', lineHeight: 1.55 }}><span style={{ color: MUTED }}>{k}：</span>{v}</div>;

class AIAnalysisHelpDoc extends Component{
	render(){
		return (
			<div style={{ marginTop: 10, borderTop: '1px solid var(--horosa-border, rgba(120,120,120,0.25))', paddingTop: 8 }}>
				<div style={title}>AI 分析 · 操作手册</div>
				<Tabs defaultActiveKey="overview" size="small">
					<TabPane tab="总览" key="overview">
						<div style={body}>
							<p style={p}><b>AI 分析</b>把你在各技法页排出并保存的命盘 / 占测快照交给你自己配置的大语言模型做解读。上方顶栏选案例、技法、模型与参数,主区是流式对话;右侧竖排页签把分析 / 历史 / 资料 / 模版 / 设置各分一栏。</p>
							<div style={card}><div style={ct}>三步上手</div>
								{kv('第一步', '「设置」页签「新增接口配置」:选供应商预设、填入你自己的接口密钥、确认模型清单;可保存多套配置随时切换')}
								{kv('第二步', '到 八字 / 紫微 / 星盘 等技法页排好盘并「保存为命盘」(占测类保存为事盘);也可在本页顶栏选「命盘时间 / 起课时间」按此刻即时起盘')}
								{kv('第三步', '顶栏「选择案例」选中它 →「选择技法」勾要挂的技法 → 输入问题发送;回答流式生成,可随时停止')}</div>
							<div style={card}><div style={ct}>隐私要点</div>
								{kv('数据去向', '发起分析时,所选案例的快照文本、引用资料与你的问题会发送给你配置的模型服务;不使用则不发送任何数据')}
								{kv('密钥存放', '接口密钥只存本机;桌面版对密钥做静态加密(AES-256-GCM)后落本地库,主密钥由系统钥匙串保管、只在会话内驻留内存')}
								{kv('换机器', '主密钥缺失时解不开的密钥一律置空、绝不当密钥发出,并提示你到接口设置重新填入')}
								{kv('完全离线', '选本地部署的模型服务(如 Ollama)可让数据完全不出本机;此类服务密钥可留空')}
								{kv('全部记录', '对话、资料、模版、接口配置都存在本机本地库,可在「设置」整包导出备份')}</div>
						</div>
					</TabPane>

					<TabPane tab="功能页签" key="panes">
						<div style={{ ...body }}>
							<p style={{ ...p, color: MUTED }}>页签在页面右侧竖排,点一下即切换;所选页签会被记住,下次进入停在原处。</p>
							<div style={card}><div style={ct}>分析</div>
								{kv('看什么', '主对话区:顶栏「选择案例」(单选,下拉切换当前分析对象)、「选择技法」(多选,可同盘合并多种技法快照)、模型 / 参数 / 连通性,下方为流式对话')}
								{kv('要点', '案例是单选;要合并多份内容,改在「选择技法」里多选,或用「挂载」按钮细调')}</div>
							<div style={card}><div style={ct}>历史</div>
								{kv('看什么', '过往对话表格,可搜索、按 Provider / 模型 / 案例类型 / 收藏 / 归档筛选,支持批量与单条操作;全部存于本机')}</div>
							<div style={card}><div style={ct}>资料</div>
								{kv('看什么', '你的个人笔记与参考资料库,可上传文档、按文件夹 / 标签 / 流派归类;对话时可作为参考挂进上下文')}</div>
							<div style={card}><div style={ct}>模版</div>
								{kv('看什么', '常用提问模版 +「组合」(绑定资料 / 默认模型 / 系统提示 / 默认技法的一键套装);模版支持多版本、版本对比与回滚')}</div>
							<div style={card}><div style={ct}>设置</div>
								{kv('看什么', '模型服务配置(多套档案)、健康概览、测试连接与连通性诊断;卡片 / 紧凑列表两种视图;可导出 / 恢复整个工作区备份')}</div>
						</div>
					</TabPane>

					<TabPane tab="分析页操作" key="analysis">
						<div style={body}>
							<div style={h}>顶栏 · 左组(模型与参数)</div>
							<ul style={ul}>
								<li style={li}><b>选择模型</b>:下拉项形如「配置名 / 模型名」,列出所有<b>已启用</b>接口配置下的聊天模型;可搜索。</li>
								<li style={li}><b>配置</b>:下拉四项 ——「配置当前接口」「拉取模型」(没有生效配置时置灰)「切换生效接口」「管理全部接口…」(跳到设置页签)。</li>
								<li style={li}><b>嵌入 / 向量模型</b>:可选、可清空;只用于资料库的向量检索,与聊天模型互相独立。不选则资料只按关键词排序。</li>
								<li style={li}><b>参数</b>:浮层设「思考档 / 温度 / top_p / 停止序列 / 频率惩罚 / 存在惩罚 / JSON 输出模式」。各项详见「接口与模型」标签。</li>
								<li style={li}><b>连通性</b>:一个可点的状态块。未配置接口时显示「未配置接口」(点开即去配置);点一下即用当前接口 + 模型发一条极短真实请求,成功显示「测试成功 · 耗时」,失败显示「测试失败」并在悬浮提示里给出原因分类与原始报错。<b>换模型或换接口后自动回灰</b>成「点击测试」——不会拿旧结果冒充新配置可用。</li>
							</ul>
							<div style={h}>顶栏 · 右组(案例与技法)</div>
							<ul style={ul}>
								<li style={li}><b>选择案例</b>:单选。列表顶部固定两个即时入口 ——「起课时间(此刻 / 自定义)」与「命盘时间(此刻 / 自定义)」,其下是已保存的命盘与事盘。<b>每次点选这两个即时入口都会把时间刷成当下此刻</b>。</li>
								<li style={li}><b>选择技法</b>:多选,按术数域分组(西方占星 / 星运推运 / 中式命理 / 占卜术数,未归域的落「其他」并排在最后)。可挂集随案例类型变化 —— 命盘案例只列命盘型技法,事盘案例只列事盘型技法;换案例后不适用的选择会自动剔除。</li>
								<li style={li}><b>挂载</b>:打开挂载抽屉,按钮右上角的数字是当前挂载层数。</li>
							</ul>
							<div style={h}>输入区</div>
							<ul style={ul}>
								<li style={li}><b>发送</b>:Enter 发送 · Shift+Enter 换行;也可点右侧圆形发送键。有文字或有待发图片才可发送。</li>
								<li style={li}><b>图片</b>:点「添加图片」,或直接把图片拖进 / 粘贴进输入框(仅视觉模型有效)。单张上限 10MB,超限者跳过并提示。</li>
								<li style={li}><b>工具排</b>:新对话 / 重新生成 / 编辑上一条并分支 / 刷新案例 / 添加图片;生成中额外出现红色「停止生成」。</li>
							</ul>
							<div style={h}>落地页建议问题</div>
							<ul style={ul}>
								<li style={li}>空对话时给出示例问题 chip:有可用接口时由 AI 按当前案例现生成三条(生成中显示提示,超时或失败静默退回静态示例);无案例时给的是「怎么用」类问题。</li>
								<li style={li}>点 chip <b>只填入输入框、不自动发送</b> ——除了「问软件怎么用」这一类:那类会自动带上软件使用说明发送,让 AI 据实回答而不是凭印象编造。</li>
								<li style={li}>若你问的是<b>某个具体命主 / 事件</b>而当前没挂案例,会先弹窗提示去挂载,不会盲发(AI 拿不到盘面只会臆测)。</li>
							</ul>
							<div style={h}>消息与会话</div>
							<ul style={ul}>
								<li style={li}><b>你的消息</b>:可「编辑」——改完基于该轮另开一条分支对话。</li>
								<li style={li}><b>任一轮</b>:可「分支」——把该轮及之前的消息复制成一条新对话(标题带「分支」),原对话不动。</li>
								<li style={li}><b>AI 回答</b>:可复制全文 / 重新生成;思考过程独立折叠(流式且正文未出时自动展开、显示「思考中…」,出正文后收起)并可单独复制;右下显示本轮输入 / 输出 token 与费用估算(价目会漂移,仅供参考)。</li>
								<li style={li}><b>出错时</b>:错误不再拼进正文,而是在气泡下方以提示条给出「原因分类 + 建议 + 原始报错」,可重试的错误直接带「重试」按钮。</li>
								<li style={li}><b>自动命名</b>:首轮回答完成后,AI 会另发一次极短请求给对话起个 6–14 字标题;失败则退回「截取首答开头」。手动重命名过的对话不再自动改名。</li>
								<li style={li}><b>整段导出</b>:会话标题右侧「导出」——Markdown / JSON / Word。</li>
								<li style={li}><b>跳到最新</b>:上滚超过一小段距离后自动暂停跟随,并浮出「跳到最新」按钮;回到底部恢复自动跟随。</li>
							</ul>
						</div>
					</TabPane>

					<TabPane tab="接口与模型" key="provider">
						<div style={body}>
							<p style={{ ...p, color: MUTED }}>本页所有模型调用都用<b>你自己的</b>接口密钥,经本机转发到你选定的服务商。</p>

							<div style={h}>供应商预设</div>
							<p style={p}>预设十余家常见服务(含通用的「自定义兼容接口」),选定即自动带出该家的默认地址、默认模型清单与默认超时;除「自定义兼容接口」外都带内置默认地址,可自行改写。</p>
							<div style={card}><div style={ct}>协议族(决定请求怎么组、哪些参数有效)</div>
								{kv('OpenAI 兼容', '最常见的一族,多数第三方与聚合服务都属此类;停止序列、频率 / 存在惩罚、JSON 输出模式仅此族生效')}
								{kv('Anthropic', '独立鉴权与版本头;思考走「思考预算」,停止序列自动映射,惩罚类参数会被丢弃')}
								{kv('Gemini', '采样参数与思考配置统一搬进生成配置块;JSON 输出会被翻成对应的响应类型')}
								{kv('本地服务(Ollama)', '采样参数统一进选项块;密钥可留空;上下文窗口按你填的窗口大小实算')}</div>

							<div style={h}>接口配置弹窗</div>
							<div style={card}><div style={ct}>基础字段</div>
								{kv('配置名称', '必填;模型下拉里显示的就是它')}
								{kv('供应商预设', '必选;切换时自动套用该家默认地址与默认模型清单')}
								{kv('API Key', '密码框;粘贴时自动清掉首尾空白与换行(防从环境文件拷贝时带入换行)。本地服务可留空')}
								{kv('Base URL', '留空则用该家内置默认地址')}
								{kv('协议族', '只读展示,随供应商预设自动判定')}
								{kv('启用此配置', '默认开;关掉后该配置下的模型不再出现在模型下拉里')}</div>
							<div style={card}><div style={ct}>高级参数(点「展开高级参数」)</div>
								{kv('聊天模型列表', '一行一个;留空则回落该家默认清单。「拉取模型」可自动获取并回填')}
								{kv('Embedding 模型列表', '同上,供向量检索用')}
								{kv('请求超时', '毫秒;各家默认 120000。留空按默认')}
								{kv('额外请求头', 'JSON 对象;用于需要非标准鉴权头的服务')}
								{kv('额外请求体 / 补充高级参数', 'JSON 对象;原样并入请求体,用于厂家私有参数')}
								{kv('校验', '这几处必须是合法 JSON / 数字,否则保存时会明确报「需要是 JSON 对象 / JSON 数组 / 有效数字」')}</div>
							<div style={card}><div style={ct}>按家显示的专属字段</div>
								{kv('Anthropic', 'API 版本(下拉,默认为推荐版本 —— 它是固定版本号、不是「当前日期」,普通用户不必改)、max_tokens、Thinking Budget、top_p、top_k')}
								{kv('Gemini', '生成配置(JSON 对象)与安全设置(JSON 数组);你手填的生成配置优先级最高,会覆盖自动映射项')}
								{kv('本地服务(Ollama)', '保活时长、上下文窗口 num_ctx、输出上限 num_predict、top_k、top_p、重复惩罚')}
								{kv('其余各家', '只有上面的通用字段')}</div>

							<div style={h}>思考档(六档)</div>
							<div style={card}>
								{kv('取值', '关闭(默认)/ 低 / 中 / 高 / 极高 / 最大')}
								{kv('落法差异', 'Anthropic → 折成「思考预算」;OpenAI 的推理型号 → 折成推理强度档(极高 / 最大均封顶为最高一档);Gemini → 折成思考预算并要求回传思维摘要;不支持标准思考参数的服务(如部分推理型号与本地服务)→ 不下发任何思考字段,静默降级')}
								{kv('影响', '档位越高越深、越慢越贵。开档后思考 token 计入输出预算 —— 若你没显式配过输出上限,系统会自动为推理模型放大预算,避免思考把正文额度吃光导致「正文空白」')}
								{kv('推理模型', '自带思考的模型会隐藏温度滑块并提示「已隐藏 temperature」;这类接口对温度有硬约束,强发会被拒')}</div>

							<div style={h}>聊天参数(顶栏「参数」浮层)</div>
							<div style={card}>
								{kv('温度 temperature', '0–2,步长 0.1;未设=不下发(用服务端默认)')}
								{kv('top_p', '0–1,步长 0.05;未设=不下发')}
								{kv('停止序列', '逗号 / 换行分隔,可留空;OpenAI 兼容与本地服务直接下发,Anthropic 自动映射到其对应字段')}
								{kv('频率惩罚 / 存在惩罚', '各 −2..2;仅 OpenAI 兼容族且非推理模型下发,其余族一律丢弃')}
								{kv('JSON 输出模式', '默认关;OpenAI 兼容族直接下发,Gemini 翻成对应响应类型,其余族丢弃')}
								{kv('保存', '这些是全局偏好,改完立刻记住,下次打开沿用')}</div>

							<div style={h}>接口管理动作</div>
							<div style={card}>
								{kv('测试连接', '发一条极短真实对话请求。没有可用聊天模型时会提示先填模型清单或点「拉取模型」;凭证类失败会明确指出去补密钥与地址')}
								{kv('连通性诊断', '分三段独立探测:域名解析 / 连接建立 / 拉取模型列表,各段给出耗时与失败原因,并附一句针对性建议')}
								{kv('拉取模型', '成功后提示拉到多少个聊天模型与多少个嵌入模型,并自动回填清单')}
								{kv('生效 API', '一个弹窗横向对比所有配置(类型 / 协议族 / 首个模型 / 地址 / 启用状态),点一下即设为当前')}
								{kv('健康概览', '设置页签顶部统计「健康 / 异常 / 未检测」三类计数')}</div>

							<div style={h}>错误分类(消息底部提示条按此分类)</div>
							<div style={card}>
								{kv('凭证问题', '密钥 / 地址不对 —— 不建议直接重试,先去设置检查')}
								{kv('上游限流', '稍候再试;建议降低并发或换模型 —— 可重试')}
								{kv('参数 / 模型问题', '检查模型选择、思考档或 JSON / 停止序列等参数 —— 不建议直接重试')}
								{kv('网络问题', '检查地址可达性 / 代理 / 防火墙 —— 可重试')}
								{kv('上游服务异常', '服务端故障,稍后重试 —— 可重试')}</div>
						</div>
					</TabPane>

					<TabPane tab="挂载与上下文" key="context">
						<div style={body}>
							<p style={{ ...p, color: MUTED }}>「上下文」= 每次发送给模型的全部内容:系统提示 + 案例快照 + 排盘规则 + 各技法判读 + 模版约束 + 引用资料 / 检索片段 + 最近对话。为避免过长被截断或超额,系统会实时统计并在超限时自动裁剪。</p>
							<div style={card}><div style={ct}>挂载状态条(常驻)</div>
								{kv('位置', '紧贴在对话输入框上方,始终显示「当前要发什么」')}
								{kv('已挂载', '📊 / 📋 案例名 · 🧮 N 技法 · 📚 N 资料 / 组合 ·「上下文约 N / 上限字」· 右侧「编辑」打开挂载抽屉')}
								{kv('未挂载', '显示「未挂载案例 · 选择案例」,点一下即打开抽屉选案例')}
								{kv('字数读数', '是「最近一次发送」实际纳入的字数与本次预算上限(上限随模型窗口变,见「算法与口径」);若有整层被丢,悬浮提示会标明层数')}</div>
							<div style={card}><div style={ct}>上下文按层组织(优先级由高到低)</div>
								{kv('系统提示', '本轮系统提示 + 组合带入的提示;最高优先,最后才会被动')}
								{kv('案例前提', '所选命盘 / 事盘的快照。未勾任何技法时挂全文,勾了技法时只挂元信息(避免与技法层重复)')}
								{kv('排盘规则', '日界点与晚子时时柱起干的当前口径。恒定挂载,让 AI 不会误读临界时刻的日柱 / 时柱')}
								{kv('使用技法', '每个勾选技法各成一层,按你的挂载顺序排;与案例前提完全相同的内容会被自动去重')}
								{kv('组合系统提示 / 模版约束', '来自「参考组合 / 资料」里选中的组合与模版')}
								{kv('检索资料片段 / 参考资料', '资料库命中的片段与直接全文挂入的资料')}
								{kv('最近对话', '按 token 预算由新往旧滚动收纳,不是固定条数')}</div>
							<div style={card}><div style={ct}>超预算时的自动裁剪</div>
								{kv('触发', '仅当上下文超出本次预算时才裁剪;不超限则原样全量发送,状态条与预览卡不出现裁剪标记')}
								{kv('超预算未纳入', '红色标签:该层整层未纳入本次发送。多见于同时挂了很多技法 —— 减少挂载技法或在该层「设置」里精简纳入内容')}
								{kv('已裁剪 X→Y 字', '橙色标签:该层按段落边界公平裁剪后纳入,X 为原字数、Y 为保留字数;不会从句子中间截断,并在结尾注明略去了哪几段')}
								{kv('裁剪次序', '必保层(系统提示 / 案例前提 / 排盘规则)优先保全 → 给模版 / 资料 / 检索 / 对话留出固定尾仓 → 各技法层按水位公平分摊;某技法层连保底字数都分不到时,宁可整层记为「未纳入」也不静默截半 —— 被整层丢弃的顺序是「最后挂载的先丢」')}</div>
							<div style={card}><div style={ct}>挂载抽屉(点顶栏「挂载」)</div>
								{kv('使用技法', '与顶栏同一份多选,分组一致')}
								{kv('参考组合 / 资料', '多选;组合会连带它绑定的资料与模版一起进上下文')}
								{kv('本轮系统提示', '自由文本,可留空;也可由「组合」一键带入')}
								{kv('本轮挂载上下文(预览)', '逐层可折叠面板,展开即看该层将发送的正文,做到所见即所发;新出现的层默认展开,你手动收起的保持收起')}</div>
							<div style={card}><div style={ct}>预览卡上的层标记</div>
								{kv('类型 / 状态', '每层标注类型(命盘 / 事盘 / 技法 / 资料 / 模版 / 系统)与状态:已就绪 / 已按盘重算 / 缺失 / 待生成')}
								{kv('已自定义', '该技法层的排盘 / 起卦设置已偏离默认;齿轮按钮同时显示为「设置 ·已改」')}
								{kv('快照或非本盘', '金色标签:该技法快照的起盘时空与当前案例对不上(可能是旧盘 / 他盘残留)。不会自动重算 —— 到该技法页按本案例重新排盘,或在该层「设置」里应用重算即可刷新')}
								{kv('签名行', '每层标题下方给出该快照的起盘时间与时区,便于核对「挂的是不是这张盘」')}
								{kv('齿轮「设置」', '技法层右侧按钮:就地打开该技法的挂载设置(见「技法挂载设置」标签)')}</div>
							<div style={card}><div style={ct}>资料怎么进上下文</div>
								{kv('短资料', '内容较短的资料整篇直接挂入,不做检索')}
								{kv('长资料', '自动切块后按你的问题做关键词粗排;若已选「嵌入 / 向量模型」,再做一次向量重排取更相关的少量片段,最后合并成一段「检索资料片段」')}
								{kv('降级', '切块失败会提示「本次检索已跳过」;向量检索失败会提示「本次仅用关键词排序」——都不阻断发送')}
								{kv('组合里的「默认检索策略」', '三档真实生效,只在挂了该组合的对话里起作用:自动=按资料长度判定(短的整篇挂、长的切块检索);全文优先=不看长度一律整篇挂,超预算时仍按裁剪规则收;检索优先=一律切块排序后只送相关片段,短资料也切。想更省预算就选检索优先,或少挂长资料')}</div>
						</div>
					</TabPane>

					<TabPane tab="技法挂载设置" key="mount">
						<div style={body}>
							<p style={{ ...p, color: MUTED }}>入口:挂载抽屉里技法层右侧的齿轮「设置」。抽屉分上下两个分区。</p>
							<div style={h}>分区一 · 该技法设置(排盘 / 起卦参数)</div>
							<div style={card}>
								{kv('内容', '按技法给出可调项(宫位制 / 黄道 / 时间算法 / 流派 / 起课体例 / 推运目标时刻 等),字段按组分列并支持条件显隐 —— 选了某档才出现的子项不会一直占位。西方占星另有「埃及历」分组(七条历轴口径可按分析覆盖);「通书择日」给流派 / 列宿用事 / 坐山三项')}
								{kv('默认即现状', '全部字段的默认值都对齐该技法页的默认值;一项都不动 = 与现状逐字一致,不触发任何重算')}
								{kv('应用并重算', '把改动写成「本次会话覆盖」并立即重算该技法快照,状态转「已按盘重算」;只影响当前会话')}
								{kv('设为同类默认', '把改动持久化为该技法的默认(以后挂载沿用),同时清掉会话覆盖以免双写歧义')}
								{kv('恢复默认', '会话覆盖与同类默认一并清除,回到出厂口径')}
								{kv('生效次序', '会话覆盖 → 该技法同类默认 → 无覆盖(默认路径)')}
								{kv('只读说明', '有些技法这一区是一段只读说明(见下),此时只能改「纳入内容」')}</div>
							<div style={h}>只能读快照、不能按挂载重算的技法</div>
							<p style={p}>下列技法的盘面是<b>一次性起出的确定性结果</b>,按挂载参数或按时间重算就等于伪造一个不同的卦 / 盘,因此挂载只读已存快照:六爻(摇钱 / 报数起卦)、统摄法、世俗盘、天文地占与塔罗(起卦 / 洗牌种子已定)、合盘(须两张盘)、辅盘(由本命盘衍生)。抽屉里会直接写出该技法的具体原因。</p>
							<p style={p}>另有一类技法本来就没有可调项(按本命盘默认参数取数),这一区会给出「按默认参数生成」的提示,同样只留内容勾选。</p>
							<div style={h}>分区二 · 纳入内容(分段勾选)</div>
							<div style={card}>
								{kv('是什么', '按该技法输出的「段」逐段勾选要不要纳入。与「AI导出设置」同一份设置——在这里改,导出也跟着变;反之亦然')}
								{kv('工具条', '全选 / 清空 / 恢复默认')}
								{kv('未自定义(默认)', '按该技法预设的全部段纳入(个别篇幅极长的典籍原文类段落默认不纳入,但可勾上)')}
								{kv('清空 = 显式全清', '存的是「一段都不要」——该技法挂载卡与导出都会变空,卡片提示会明确写「所有分段已在纳入内容中取消」,与「找不到快照」区分开')}
								{kv('恢复默认', '删掉该技法的自定义,回到预设全段')}
								{kv('段很多的技法', '会按右栏语义分成小组显示(未命中分组规则的段落落「其他段」,永远不会被藏起来)')}
								{kv('即时生效', '改完立刻重取挂载内容刷新卡片,不必重新排盘')}
								{kv('附加勾选', '星曜类技法另有「显示星曜宫位 / 显示星曜主宰宫」两项(默认都开),以及部分技法的「在对应分段输出释义」(默认关)')}
								{kv('尚未排盘', '若该技法还没排过一次盘,这一区会提示「暂未检测到可选分段」——先去该技法页排一次即可')}</div>
							<div style={h}>两个即时起盘入口(在案例下拉顶部)</div>
							<div style={card}><div style={ct}>起课时间 · 即时起式盘</div>
								{kv('用法', '填时间(「此刻」一键取当前)、📍 选地点(自动带出时区)、微调经纬与时区、填事由与性别')}
								{kv('可起什么', '卜卦 / 报数 / 起例类的时间确定式法都能凭时间 + 地点即时起盘')}
								{kv('不能起什么', '统摄法 / 宿占 / 世俗盘需先手动起盘并存为事盘再挂载 —— 凭时间起只会得到无意义的默认值,故不在白名单,会显示「缺失」')}
								{kv('一键挂载全部式法', '一次挂满可即时起盘的式法;奇门遁甲不随一键加入,需手动勾选')}
								{kv('保存为事盘', '把当前草稿存进案例列表并自动选中,以后可复用')}</div>
							<div style={card}><div style={ct}>命盘时间 · 即时起命盘</div>
								{kv('用法', '同上,但填的是出生时间与姓名')}
								{kv('可起什么', '按默认设置即时起命盘类技法(八字 / 紫微 / 星盘 / 各推运)')}
								{kv('保存为命盘', '存进案例列表并自动选中')}</div>
						</div>
					</TabPane>

					<TabPane tab="历史 · 资料 · 模版" key="manage">
						<div style={body}>
							<div style={card}><div style={ct}>历史页</div>
								{kv('筛选', '搜索(标题 / 案例名 / 模型)+ Provider / 模型 / 案例类型(命盘·事盘)/ 收藏(全部·仅收藏·未收藏)/ 归档(未归档·已归档·全部)多面过滤;默认只看未归档')}
								{kv('批量', '勾选后可批量导出 / 归档 / 取消归档 / 收藏 / 取消收藏 / 删除')}
								{kv('单条', '打开 / 重命名 / 复制 / 收藏 / 归档 / 导出(Markdown·JSON·Word)/ 删除')}
								{kv('批量导出', '打成一个压缩包,每段会话各出一份 Markdown 与一份 JSON,并附一张清单')}
								{kv('删除', '删的是当前正在生成的会话时,会连带停止生成')}</div>
							<div style={card}><div style={ct}>资料页</div>
								{kv('上传', '把文件拖到本面即上传,或点「选文件上传」(可多选);支持 TXT / Markdown / DOC / DOCX / PDF。上传过程逐份显示「解析中 / 导入中 / 完成 / 已跳过 / 失败」')}
								{kv('桌面版额外', '「桌面选文件」与「导入目录」可整目录批量入库')}
								{kv('新建资料', '手动录入:名称(必填)、文件夹、标签(逗号分隔)、流派(可多选 / 自由输入,不填=通用资料)、正文(必填)')}
								{kv('组织', '按文件夹归类;卡片 / 列表两种视图;按更新时间 / 名称 / 大小排序;「管理文件夹」可新建 / 重命名 / 删除(删文件夹时内含资料自动移到未分类)')}
								{kv('去重', '一键检出重复资料并提示处理')}
								{kv('单条', '编辑 / 加入参考 / 移动文件夹 / 导出(原文件·提取文本)/ 替换文件 / 删除')}
								{kv('标签组', '若存在标签组会在顶部只读展示,便于回顾自己的标签体系')}</div>
							<div style={card}><div style={ct}>模版页 · 模版</div>
								{kv('两种格式', '文字模版 / JSON 模版(后者可写 JSON Schema,并对示例输出做校验)')}
								{kv('可用变量', '{{user_prompt}} / {{source_context}} / {{retrieved_context}} / {{conversation_history}} / {{system_prompt}}')}
								{kv('变量推断', '编辑时右侧实时列出你用到的变量,不在已知列表里的会标问号并提示「发送时可能解析不到」')}
								{kv('预览', '用示例输入渲染出实际会发出的文本;JSON 模版同时给出 Schema 校验结果')}
								{kv('版本', '每次保存自动留版本;两个及以上版本时可「版本对比」(逐行差异 + 增删统计),并从版本列表任选一版「回滚」')}</div>
							<div style={card}><div style={ct}>模版页 · 组合</div>
								{kv('是什么', '一键套装:绑定模版 + 默认资料 + 默认模型 / 嵌入模型 + 默认系统提示 + 默认挂载技法 + 默认生成参数(温度 / top_p / 思考档,留空=不覆盖)')}
								{kv('预览', '先看「套用后会设置什么」;技法一栏会按当前案例算出「可挂载几项」与「跳过几项(不适用当前案例类型)」')}
								{kv('一键应用', '写入参考、模型、系统提示与生成参数,并把默认技法暂存 —— 等你选定案例后按交集自动挂上')}
								{kv('加入参考', '只把该组合加进本轮参考,不改其它设置')}</div>
						</div>
					</TabPane>


					<TabPane tab="算法与口径" key="algo">
						<div style={body}>
							<p style={{ ...p, color: MUTED }}>本页只写判据与边界,便于你判断「为什么少了内容 / 为什么停了 / 为什么标了警告」。</p>

							<div style={h}>上下文预算怎么算</div>
							<div style={card}>
								{kv('未知模型', '按保底 20000 字')}
								{kv('已知模型', '按该模型上下文窗口实算:先留出四分之一窗口(下限 1024、上限 16384 token)给输出,余量的六成折成字数(中文约 1.6 字 / token),再夹在 2000–60000 字之间')}
								{kv('本地服务', '按你在配置里填的上下文窗口实算;窗口很小时预算会低于保底 —— 这是有意的,防止往小窗口硬塞两万字被引擎静默截断')}
								{kv('token 估算', '中日韩字符按约 1.6 字 / token、其余字符按约 4 字 / token 分别估算再相加(旧口径一律除四会把中文低估约两倍半)')}</div>

							<div style={h}>公平分摊裁剪</div>
							<div style={card}>
								{kv('不超限', '整段快路径,原样全发,与不裁剪逐字一致')}
								{kv('必保层', '系统提示 / 案例前提 / 排盘规则先足额收纳;实在放不下才做部分截断')}
								{kv('尾仓', '给模版 / 资料 / 检索 / 最近对话预留一块固定比例的尾仓(约预算的一成半),技法层不许吃光全部余量')}
								{kv('技法层', '每层保底约 600 字;预算连保底都给不起时,从「最后挂载的那一层」起整层丢弃(有账可查,不静默),直到剩下的层都能拿到保底')}
								{kv('水位分摊', '剩下的技法层按内容长度升序做水位分摊 —— 短层全保,长层按水位在段边界截断,并在结尾注明略去了哪几段')}</div>

							<div style={h}>最近对话怎么带</div>
							<div style={card}>
								{kv('规则', '由新往旧收,累计约 4000 token 即止')}
								{kv('保底', '最近 4 条无论多长都带上(保证最近两轮问答完整)')}
								{kv('上限', '最多 40 条,防级联膨胀')}</div>

							<div style={h}>资料检索链</div>
							<div style={card}>
								{kv('直挂门槛', '正文长度在约一万二千字以内的资料整篇直接挂入,不进检索')}
								{kv('切块', '超过门槛的资料按约 1200 字一块切分,相邻块留约 180 字重叠(防判据被切两半)')}
								{kv('粗排', '按问题做关键词排序,取前十余块')}
								{kv('重排', '配了嵌入模型时对粗排结果做一次向量重排,收到更少更相关的几块')}
								{kv('合并', '最终合并为一段有字数上限的「检索资料片段」层')}</div>

							<div style={h}>流控 · 超时 · 停止</div>
							<div style={card}>
								{kv('传输', '流式返回,逐块渲染;服务端每十几秒发一次保活帧,防中间环节因空闲断连')}
								{kv('空闲看门狗', '90 秒内没有真正的新内容就判为卡住并停止等待 —— 保活帧不算「有产出」,但思维链增量算(推理模型可以先长时间只思考)')}
								{kv('单次硬上限', '单次生成最多 5 分钟,超过即停止等待并提示可重新生成')}
								{kv('停止生成', '点一下立即断流:发起前已停就不发,读流中每轮双重检查,并直接取消底层流。停止后本条标为「已停止」,不重试、不算失败、不记错误')}
								{kv('请求超时', '各家默认 120 秒,可在接口高级参数里改')}</div>

							<div style={h}>快照与案例是否同一张盘</div>
							<div style={card}>
								{kv('比对项', '起盘时间(精确到分钟)、时区、经度、纬度(各保留一位小数以消除格式噪声)')}
								{kv('判据', '所有可比项全等 → 视为同盘;任一项不等 → 标「快照或非本盘」')}
								{kv('宁漏勿误伤', '任一侧信息不足即判为「无法判断」,绝不报警 —— 事盘快照通常不带出生位,所以多数事盘不会出现该标签')}
								{kv('不自动重算', '标了也不会自动重算 —— 重算等于替你改盘;由你决定是回技法页重排还是在挂载设置里应用重算')}</div>

							<div style={h}>案例快照怎么来</div>
							<div style={card}>
								{kv('命盘案例', '按出生数据无头重算:排一次盘再按技法生成结构化快照,状态显示「已按盘重算」')}
								{kv('事盘案例', '只认该事盘自己存下的内容;缺失时仅对「时间确定式法」按其起课时间补算,其余显示「缺失」')}
								{kv('绝不按时间复算', '摇卦 / 报数 / 洗牌类的起课是一次性的,按时间重算会得到一个不同的卦 —— 这是硬规则,不是缺省')}
								{kv('合盘与折线类', '只读对应页面最近一次保存的结果(前者需两张盘,后者需该页算过一次)')}
								{kv('全局缓存的取用条件', '只有起盘签名确凿匹配当前案例时才复用页面留下的快照,否则一律重算 —— 宁可多算一次也绝不挂错盘')}</div>

							<div style={h}>显示与文本规范</div>
							<div style={card}>
								{kv('波浪号', '正文里的「~」一律换成短横线 —— 否则年龄 / 年份区间会被 Markdown 当成删除线整段划掉;代码块内的波浪号保持原样')}
								{kv('标题粘连', '自动修复「句末紧跟井号标题」这类粘连,但不会把连续井号拆开')}
								{kv('公式', '支持行内与块级数学公式渲染')}</div>

						</div>
					</TabPane>

					<TabPane tab="注意点" key="caveats">
						<div style={body}>
							<div style={card}><div style={ct}>挂载相关</div>
								{kv('事盘绝不按时间重算', '摇卦 / 报数 / 洗牌类事盘只认存盘。若显示「缺失」,正确做法是回该技法页按本案例起一次并保存,而不是期待挂载替你算')}
								{kv('两种「空」要分清', '「所有分段已在纳入内容中取消」是你自己清空了段;「未找到该技法可用快照」才是真的没排过盘 —— 别把前者当故障去排查排盘')}
								{kv('清空 ≠ 未设置', '「纳入内容」点了清空就是「一段都不要」,会一直生效直到你重新勾选或点「恢复默认」')}
								{kv('改了设置没反应', '改「该技法设置」后要点「应用并重算」才会重算;只改「纳入内容」则即时生效、无需重算')}
								{kv('挂太多技法会互相挤', '上下文有预算上限,技法层是最先被裁 / 被丢的一类,且「最后挂载的先丢」——重要技法建议先挂')}</div>
							<div style={card}><div style={ct}>模型与参数相关</div>
								{kv('参数不是每家都生效', '停止序列 / 频率与存在惩罚 / JSON 输出只对 OpenAI 兼容族完整生效;其余族会被丢弃或改写,浮层底部已注明')}
								{kv('推理模型的温度', '这类模型隐藏温度滑块 —— 不是控件坏了,是接口本身不允许')}
								{kv('思考档会吃输出额度', '开高档时若你手动配过很小的输出上限,可能出现「只思考、正文空」——把输出上限调大即可')}
								{kv('测试成功不等于永远可用', '状态块只代表「当前接口 + 当前模型」的那一次测试;换模型即回灰,请重测')}
								{kv('费用只是估算', '价目会漂移,token 数是真实回传值、金额是按内置价目表估算;没有价目的模型只显示 token')}</div>
							<div style={card}><div style={ct}>数据与备份相关</div>
								{kv('恢复备份是整包覆盖', '会先清空现有工作区再写入备份内容 —— 恢复前请先导出一份当前备份')}
								{kv('换机器要重填密钥', '密钥的主密钥由本机系统钥匙串保管,不随备份迁移;换机后到接口设置重新粘一次即可')}
								{kv('图片有上限', '单张 10MB;超限者会被跳过并提示。图片只对视觉模型有效')}
								{kv('标签组是只读展示', '资料页顶部的标签组用于回顾已有标签体系,不在本页新建')}</div>
							<div style={card}><div style={ct}>易误解的控件</div>
								{kv('组合的「默认检索策略」', '自动=按资料长度判定(见「算法与口径」的阈值);全文优先=强制整篇直挂,不看长度;检索优先=强制切块+排序,短资料也切。挂了多个组合时,取第一个不是「自动」的那个为准;都是「自动」则按长度走')}
								{kv('「AI助手」快捷按钮', '各技法页底部的这个按钮只是跳转到 AI 分析页,不会把当前盘变成案例;要成为可挂载案例须先在技法页「保存为命盘 / 事盘」')}
								{kv('顶栏「AI导出」', '那是技法页的导出入口(把当前页判读文本复制 / 导出成 TXT·Word·PDF),与本页对话无关;它的「AI导出设置」与本页「纳入内容」是同一份设置')}</div>
						</div>
					</TabPane>
				</Tabs>
			</div>
		);
	}
}

export default AIAnalysisHelpDoc;
