diff --git a/astrostudyui/src/components/ruleziwei/RuleHouses.js b/astrostudyui/src/components/ruleziwei/RuleHouses.js
index f07496d..4e72abb 100644
--- a/astrostudyui/src/components/ruleziwei/RuleHouses.js
+++ b/astrostudyui/src/components/ruleziwei/RuleHouses.js
@@ -1,7 +1,9 @@
 import { Component } from 'react';
 import { Row, Col, Divider, Popover, } from 'antd';
-import {randomStr,} from '../../utils/helper';
 import * as ZWConst from '../../constants/ZWConst';
+// horosa_stable_react_keys_v1(PERF-R9):本文件的 React key 已从 randomStr(8) 改为内容派生的稳定 key。
+// 随机 key 每次渲染都变 → React 无法 diff → 整棵子树卸载重建。此标记供 apply.sh 的
+// 幂等守卫与发布哨兵定位;删除它会让重同步后无法自动还原本改动。
 import STAR_MEANING from '../ziwei/data/tables/ziweiStarMeaning.json';   // WP-7 十二宫含义
 
 class RuleHouses extends Component{
@@ -29,7 +31,7 @@ class RuleHouses extends Component{
 			let dom = this.genPopoverDom(rules, house);
 			let title = house + '';
 			let col = (
-				<Col span={6} key={randomStr(8)}>
+				<Col span={6} key={house}>
 					<Popover content={dom} title={title}>
 						{house}
 					</Popover>					
@@ -43,7 +45,7 @@ class RuleHouses extends Component{
 	genPopoverDom(rules, house){
 		let meaning = house ? STAR_MEANING.houses[house] : null;   // WP-7 十二宫含义
 		let head = meaning ? (
-			<div key={randomStr(8)} style={{ marginBottom: 8, padding: '6px 9px', background: 'var(--horosa-ziwei-selected-bg, rgba(120,72,232,0.08))', borderRadius: 4, fontSize: 12, lineHeight: '20px' }}>{meaning}</div>
+			<div key="meaning" style={{ marginBottom: 8, padding: '6px 9px', background: 'var(--horosa-ziwei-selected-bg, rgba(120,72,232,0.08))', borderRadius: 4, fontSize: 12, lineHeight: '20px' }}>{meaning}</div>
 		) : null;
 		let lis = [];
 		for(let i=0; i<rules.length; i++){
@@ -63,16 +65,16 @@ class RuleHouses extends Component{
 					)
 				}else{
 					li = (
-						<li key={randomStr(8)}>{rule}</li>
+						<li key={i}>{rule}</li>
 					);	
 				}
 			}
 			lis.push(li);
 		}
 		let rulesDom = (
-			<div key={randomStr(8)} style={{width: 400}}>
+			<div key="rules" style={{width: 400}}>
 				{head}
-				<ul key={randomStr(8)}>
+				<ul key="list">
 					{lis}
 				</ul>
 			</div>
@@ -93,7 +95,7 @@ class RuleHouses extends Component{
 			let rules = ZWRules.RuleHouseType[key];
 			let dom = this.genPopoverDom(rules);
 			let col = (
-				<Col span={6} key={randomStr(8)}>
+				<Col span={6} key={key}>
 					<Popover content={dom} title={key}>
 						{key}
 					</Popover>					
