diff --git a/astrostudyui/src/components/ruleziwei/RuleHuaDesc.js b/astrostudyui/src/components/ruleziwei/RuleHuaDesc.js
index 943b6ec..318db5d 100644
--- a/astrostudyui/src/components/ruleziwei/RuleHuaDesc.js
+++ b/astrostudyui/src/components/ruleziwei/RuleHuaDesc.js
@@ -1,7 +1,9 @@
 import { Component } from 'react';
 import { Row, Col, Divider, Popover, } from 'antd';
-import {randomStr,} from '../../utils/helper';
 import * as ZWConst from '../../constants/ZWConst';
+// horosa_stable_react_keys_v1(PERF-R9):本文件的 React key 已从 randomStr(8) 改为内容派生的稳定 key。
+// 随机 key 每次渲染都变 → React 无法 diff → 整棵子树卸载重建。此标记供 apply.sh 的
+// 幂等守卫与发布哨兵定位;删除它会让重同步后无法自动还原本改动。
 
 class RuleHuaDesc extends Component{
 	constructor(props) {
@@ -19,7 +21,7 @@ class RuleHuaDesc extends Component{
 			let rules = huas[key];
 			let dom = this.genPopoverDom(rules);
 			let col = (
-				<Col span={8} key={randomStr(8)}>
+				<Col span={8} key={key}>
 					<Popover content={dom} title={key}>
 						{key}
 					</Popover>					
@@ -37,7 +39,7 @@ class RuleHuaDesc extends Component{
 			let li = null;
 			if(rule === '=='){
 				li = (
-					<hr key={randomStr(8)} />
+					<hr key={i} />
 				);
 			}else{
 				if(rule instanceof Array){
@@ -51,15 +53,15 @@ class RuleHuaDesc extends Component{
 					)
 				}else{
 					li = (
-						<li key={randomStr(8)}>{rule}</li>
+						<li key={i}>{rule}</li>
 					);	
 				}
 			}
 			lis.push(li);
 		}
 		let rulesDom = (
-			<div key={randomStr(8)} style={{width: 400, height:400, overflow: 'auto'}}>
-				<ul key={randomStr(8)}>
+			<div key="rules" style={{width: 400, height:400, overflow: 'auto'}}>
+				<ul key="list">
 					{lis}
 				</ul>
 			</div>
@@ -80,7 +82,7 @@ class RuleHuaDesc extends Component{
 			let dom = this.genPopoverDomInObj(hua);
 			let title = key + '在命盘'
 			let col = (
-				<Col span={12} key={randomStr(8)}>
+				<Col span={12} key={key}>
 					<Popover content={dom} title={title}>
 						<span style={ZWConst.SihuaColor[key]}>{title}</span>						
 					</Popover>					
@@ -101,18 +103,18 @@ class RuleHuaDesc extends Component{
 				let li = null;
 				if(rule === '=='){
 					li = (
-						<hr key={randomStr(8)} />
+						<hr key={i} />
 					);
 				}else{
 					li = (
-						<li key={randomStr(8)}>{rule}</li>
+						<li key={i}>{rule}</li>
 					);
 				}
 				houslis.push(li);
 			}
 
 			let house = (
-				<li key={randomStr(8)}>
+				<li key={key}>
 					<div>
 						{key}
 					</div>
@@ -123,8 +125,8 @@ class RuleHuaDesc extends Component{
 		}
 
 		let rulesDom = (
-			<div key={randomStr(8)} style={{width: 400, height:400, overflow: 'auto'}}>
-				<ul key={randomStr(8)}>
+			<div key="rules" style={{width: 400, height:400, overflow: 'auto'}}>
+				<ul key="list">
 					{lis}
 				</ul>
 			</div>
