diff --git a/astrostudyui/src/components/comp/EditableTags.js b/astrostudyui/src/components/comp/EditableTags.js
index f555442..7a96ddd 100644
--- a/astrostudyui/src/components/comp/EditableTags.js
+++ b/astrostudyui/src/components/comp/EditableTags.js
@@ -1,8 +1,10 @@
 import { Component } from 'react';
 import { Tag, Popconfirm, } from 'antd';
-import {randomStr} from '../../utils/helper';
 import { XQInput } from '../xq-ui';
 import XQIcon from '../xq-icons';
+// horosa_stable_react_keys_v1(PERF-R9):本文件的 React key 已从 randomStr(8) 改为内容派生的稳定 key。
+// 随机 key 每次渲染都变 → React 无法 diff → 整棵子树卸载重建。此标记供 apply.sh 的
+// 幂等守卫与发布哨兵定位;删除它会让重同步后无法自动还原本改动。
 
 
 class EditableTags extends Component{
@@ -55,7 +57,7 @@ class EditableTags extends Component{
 		const txt = tag.label === undefined ? tag : tag.label;
 
 		let tagdom = (
-			<Tag key={randomStr(8)} closable={editable}
+			<Tag key={index} closable={editable}
 				onClose={(e) => this.closeTag(tag)}
 			>
 				{txt}
@@ -66,11 +68,11 @@ class EditableTags extends Component{
 			if(editable){
 				tagdom = (
 					<Popconfirm
-						key={randomStr(8)} 
+						key={index}
 						title={`确定删除 ${txt} 吗？`}
 						onConfirm={(e)=>this.closeTag(tag)}
 					>
-						<Tag key={randomStr(8)} closable={false}>
+						<Tag key='tag' closable={false}>
 							{txt}
 						</Tag>
 					</Popconfirm>
@@ -78,7 +80,7 @@ class EditableTags extends Component{
 				);				
 			}else{
 				tagdom = (
-					<Tag key={randomStr(8)} closable={false}>
+					<Tag key={index} closable={false}>
 						{txt}
 					</Tag>
 				);
@@ -166,7 +168,7 @@ class EditableTags extends Component{
 				}
 				{
 					editable && !inputVisible && (
-						<Tag key={randomStr(8)} onClick={this.showInput} style={{ background: 'var(--horosa-panel-bg, #fff)', borderStyle: 'dashed' }}>
+						<Tag key='new-tag' onClick={this.showInput} style={{ background: 'var(--horosa-panel-bg, #fff)', borderStyle: 'dashed' }}>
 							<XQIcon name="plus" /> {this.props.newTagLabel}
 						</Tag>
 					)
