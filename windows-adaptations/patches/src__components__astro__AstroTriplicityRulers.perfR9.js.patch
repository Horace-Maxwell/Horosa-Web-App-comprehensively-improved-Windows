diff --git a/astrostudyui/src/components/astro/AstroTriplicityRulers.js b/astrostudyui/src/components/astro/AstroTriplicityRulers.js
index 3836341..137d764 100644
--- a/astrostudyui/src/components/astro/AstroTriplicityRulers.js
+++ b/astrostudyui/src/components/astro/AstroTriplicityRulers.js
@@ -6,6 +6,7 @@ import { buildTriplicityPeriods, buildTriplicityRulersSnapshotText, TRIPLICITY_D
 import { symbolWithMeaning } from './AstroExtraCommon';
 import { XQSelect as Select, XQSegmented } from '../xq-ui';
 import styles from '../../css/styles.less';
+import { markPanelReady } from '../../utils/perfMark';
 
 const Option = Select.Option;
 // 三分体系切换项（默认多罗特＝现状）。
@@ -39,7 +40,11 @@ class AstroTriplicityRulers extends Component{
 		}
 		if(prevProps.value !== this.props.value || prevProps.showAstroMeaning !== this.props.showAstroMeaning){
 			this.rebuild();
-			if(prevProps.value !== this.props.value){ this.saveAISnapshot(); }
+			if(prevProps.value !== this.props.value){
+				// horosa_panel_ready_v1:本技法无自有请求,数据整份派生自 chartObj —— 「落定」= 拿到新盘那次提交。
+				markPanelReady('direction');
+				this.saveAISnapshot();
+			}
 		}
 	}
 
